import { SearchPageComponent } from "./styles.module.scss";

interface Props {}

function SearchPage(props: Props) {
  const {} = props;

  return (
    <SearchPageComponent>
      <h1>Search</h1>
    </SearchPageComponent>
  );
}

export default SearchPage;
